import { StyleSheet } from 'react-native';

const fontBold = 'DMSans-Bold';
const fontMedium = 'DMSans-Medium';
const fontRegular = 'DMSans-Regular';
const fontSemiBold = 'DMSans-SemiBold';

const styles = {
    fontBold: {
        fontFamily: fontBold,
    },
    fontMedium: {
        fontFamily: fontMedium,
    },
    fontRegular: {
        fontFamily: fontRegular,
    },
    fontSemiBold: {
        fontFamily: fontSemiBold,
    },
};

export const fonts = StyleSheet.create(styles);
